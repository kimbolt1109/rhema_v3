{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Renderer side: React 19 UI + the shared protocol types. Checked by `npm run typecheck:web`. `@main/*` is deliberately NOT mapped here — the renderer must never import main-process code; it talks to main only through the preload IPC bridge.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client"],
    "paths": {
      "@shared/*": ["src/shared/*"],
      "@renderer/*": ["src/renderer/*"]
    }
  },
  "include": [
    "src/renderer/**/*.ts",
    "src/renderer/**/*.tsx",
    "src/renderer/**/*.d.ts",
    "src/shared/**/*.ts"
  ],
  "exclude": ["node_modules", "out", "release", "dist"]
}
